namespace UOStudio.Client.Engine.World
{
    public class MapRenderer
    {
        private readonly Map _map;

        public MapRenderer(Map map) => _map = map;

        public void Draw(Camera camera)
        {
        }
    }
}
